# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/hoadoor?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-change-in-production"

# Email (for magic links)
EMAIL_SERVER_USER="hoadoor@localhost"
EMAIL_SERVER_PASSWORD=""
EMAIL_SERVER_HOST="localhost"
EMAIL_SERVER_PORT="1025"
EMAIL_FROM="noreply@hoadoor.com"

# Optional: Redis for rate limiting (fallback to database if not provided)
# UPSTASH_REDIS_REST_URL=""
# UPSTASH_REDIS_REST_TOKEN=""

# Application
NODE_ENV="development"
